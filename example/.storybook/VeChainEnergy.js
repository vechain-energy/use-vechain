// .storybook/YourTheme.js

import { create } from '@storybook/theming';

export default create({
  base: 'light',
  brandTitle: '@vechain.energy/use-vechain',
  brandUrl: 'https://vechain.energy',
  brandTarget: '_self',
});